Rule: react-doctor/no-giant-component
Severity: warning
Category: Maintainability
Count: 3

Component "BillGenerator" is over 300 lines long, which is hard to read & change. Split it into a few smaller components.

Component "Home" is over 300 lines long, which is hard to read & change. Split it into a few smaller components.

Component "InvoiceGenerator" is over 300 lines long, which is hard to read & change. Split it into a few smaller components.

Suggestion: Pull each section into its own component so the parent is easier to read, test, and change.

Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-giant-component

Files:
  src/pages/BillGenerator.tsx:10
  src/pages/Home.tsx:58
  src/pages/InvoiceGenerator.tsx:67
